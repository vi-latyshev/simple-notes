import type { NextPage } from 'next';

import { Page } from 'components/Page';

const Home: NextPage = () => {
    const a = '';

    return (
        <Page>
            { }
        </Page>
    );
};

export default Home;
