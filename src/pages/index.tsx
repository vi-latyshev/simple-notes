import type { NextPage } from 'next';

import { Page } from 'components/Page';

const Home: NextPage = () => {
    const a = '';

    return (
        <Page>
            test
        </Page>
    );
};

export default Home;
